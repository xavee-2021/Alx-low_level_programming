#include "holberton.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - check the length of a string.
 *
 * @s: passed in parameter.
 *
 * Return: length.
 */
int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
